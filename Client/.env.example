# Local client server
CLIENT_PORT=5172

# Student PMS API server
API_SERVER_URL=https://student-project-management-system-jtla.onrender.com
API_PREFIX=/api
UPLOADS_PREFIX=/uploads

# Leave blank locally to use Vite's proxy. In production, set this to the
# public origin of the deployed API, for example https://api.example.com.
VITE_API_ORIGIN=
